# Tic Tac Toe (Minimal)

A tiny no-build Tic Tac Toe game with a minimal UI and distinct colors for **X** and **O**.

## Run
Open `index.html` in your browser.

If you prefer a local server:

```bash
python3 -m http.server 5173
# open http://localhost:5173
```
